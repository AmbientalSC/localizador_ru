// Exemplo de configuração local para chaves de API.
// Copie este arquivo para data/config.js e adicione a sua chave. NÃO commit this file with sua chave.

// Em production, prefira injetar a chave no processo de build ou via variáveis de ambiente.

window.GOOGLE_API_KEY = 'YOUR_GOOGLE_API_KEY_HERE';

// Depois insira 'data/config.js' em seu .gitignore para evitar commits acidentais.
