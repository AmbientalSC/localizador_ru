// Exemplo de configuração local para chaves de API.
// Copie este arquivo para data/config.js e adicione a sua chave. NÃO commite este arquivo com a chave.
// Em produção, prefira injetar a chave no processo de build ou via variáveis de ambiente.

// Substitua o texto abaixo pela sua chave durante testes locais:
window.GOOGLE_API_KEY = 'YOUR_GOOGLE_API_KEY_HERE';

// Depois insira 'data/config.js' em seu .gitignore para evitar commits acidentais.
